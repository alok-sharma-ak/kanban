meta {
  name: Delete second task
  type: http
  seq: 8
}

delete {
  url: {{HostUrl}}/tasks/PASTE_TASK_ID_HERE
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{AuthKey}}
}

tests {
  test("deletes the remaining task", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
