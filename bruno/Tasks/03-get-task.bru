meta {
  name: Get task
  type: http
  seq: 3
}

get {
  url: {{HostUrl}}/tasks/{{FirstTaskId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{AuthKey}}
}

tests {
  test("returns the task", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().id).to.equal(bru.getVar("FirstTaskId"));
  });
}
