meta {
  name: Delete temporary board
  type: http
  seq: 2
}

delete {
  url: {{HostUrl}}/boards/PASTE_BOARD_ID_HERE
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{AuthKey}}
}

tests {
  test("deletes the temporary board", function () {
    expect(res.getStatus()).to.equal(204);
  });
}
