meta {
  name: List boards
  type: http
  seq: 2
}

get {
  url: {{HostUrl}}/boards
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{AuthKey}}
}

tests {
  test("lists accessible boards", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody()).to.be.an("array");
  });
}
